calling the kestra start the docker 
   docker run --pull=always --rm -it -p 8080:8080 --user=root `
     -v //var/run/docker.sock:/var/run/docker.sock `
     -v ${env:TEMP}:/tmp `
     kestra/kestra:latest server local

docker command to call kestra

docker run --pull=always --rm -it -p 8080:8080 --user=root \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v /tmp:/tmp \
  kestra/kestra:latest server local     


  ##then create a flow and add claimguardian-workflow.yaml then execute 